핵심 
-- 현재까지 구해진 값을 Set에 저장(중복 자동 제외)
-- 이번 점수를 Set에 있는 모든 값에 더하여 각각 저장



생각만 한점.
--set을 쓰지 않고 100001(100 * 100 +1) 배열을 잡아서 이전에 더한 값들의 최대값까지 순회
--방문한 적이 있다면 index가 곧 값이니깐 그 index에 이번 점수를 더하여 배열[index+point[이번]] = true;
